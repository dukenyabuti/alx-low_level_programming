#include "main.h"

/**
 * _abs -check main
 * @x: An integer input
 * Description: it computes value of integer
 * Return: ABsolute value of x
 */
int _abs(int x)
{

	if (x >= 0)
		return (x);
	else
		return (x * -1);
}
